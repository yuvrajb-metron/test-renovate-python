# Test dependencies for Renovate
# These packages are intentionally set to older versions so Renovate can detect updates

# Web framework
flask==2.0.1
requests==2.26.0

# Data processing
pandas==2.3.3
numpy==1.21.0

# Utilities
python-dateutil==2.8.1
pytz==2021.1

# Testing (optional)
pytest==6.2.4
